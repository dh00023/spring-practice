server:
  port: 8888

spring:
  application:
    name: config-service
  #  profiles:
  #    active: native
  cloud:
    config:
      server:
        git:
          uri: git@github.com:dh00023/secret.git
          strict-host-key-checking: false
